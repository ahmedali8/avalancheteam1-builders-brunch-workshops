# Foundry
out/
cache/
# Deployment receipts are worth committing — they're the record of what went to Fuji.
# Local anvil runs (31337) and simulations are noise, so those two stay out.
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# bun / deps
node_modules/

# secrets
.env
